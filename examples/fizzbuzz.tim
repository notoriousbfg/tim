(fizzBuzz: (number) => {
    ?(
        (number % 15) => ('fizzbuzz').print(),
        (number % 3) => ('fizz').print(),
        (number % 5) => ('buzz').print()
    )
})

().range(1, 100).each(fizzBuzz)